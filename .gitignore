# --- Local runtime data (do not commit test uploads / db) ---
static/uploads/*
!static/uploads/.gitkeep
data/database.json

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# --- IDE / OS ---
.idea/
.vscode/
.DS_Store
Thumbs.db
/static/maps
/data/map_cache
/data
data/database.json
notes.md

# --- JS test tooling (visual regression + perf profiling) ---
node_modules/
test-results/
playwright-report/
# Generated fixtures + profiler output are reproducible from the scripts.
tests/visual/fixtures/
tests/perf/fixtures/
tests/perf/results/
# Golden screenshots are generated per-machine on first run (see tests/README).
# Commit them deliberately once reviewed if you want CI to diff against them.
tests/visual/__screenshots__/
